name: SentinelPR
description: Enterprise Java Security & PR Review Copilot powered by Shree AI OS

branding:
  icon: shield
  color: blue

inputs:
  github_token:
    description: GitHub token for PR comments
    required: true

  gemini_api_key:
    description: Gemini BYOK API key
    required: false

  fail_on_critical:
    description: Fail workflow on CRITICAL findings
    required: false
    default: "true"

  format:
    description: Output format
    required: false
    default: github

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: "21"

    - name: Download SentinelPR
      shell: bash
      run: |
        curl -L \
          https://github.com/darshanrathod04/sentinel-pr/releases/download/v1.1.3/sentinel-pr-1.1.3.jar \
          -o sentinel-pr.jar

    - name: Run SentinelPR
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        GEMINI_API_KEY: ${{ inputs.gemini_api_key }}
      run: |
        java -Dloader.main=com.sentinelpr.cli.SentinelCliRunner \
          -cp sentinel-pr.jar org.springframework.boot.loader.launch.PropertiesLauncher \
          --target . \
          --repo ${{ github.repository }} \
          --pr ${{ github.event.pull_request.number }} \
          --post-comment \
          -f ${{ inputs.format }}
